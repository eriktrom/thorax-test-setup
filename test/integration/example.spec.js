define(function(require) {
  describe('adding an item to the todo list', function () {
    it('can be done', function () {
      // var TodoList = require('routers/todo-list');
      // var todoList = new TodoList();

      // TODO: currently fails b/c of coffeescript
      // compilation error, only from karma
    });
  });
});